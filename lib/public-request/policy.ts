export const EMAIL_OTP_LENGTH=8;
export function validCanonicalEmailOtp(value:unknown){return new RegExp(`^\\d{${EMAIL_OTP_LENGTH}}$`).test(String(value??''))}
export function normalizePublicRequestEmail(value:unknown){return String(value??'').trim().toLowerCase()}
export function validPublicRequestEmail(value:string){return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(value)&&value.length<=254}
export function validatePublicEnquiry(type:string,payload:any){if(type==='individual_product'){if(!String(payload.partNumber??'').trim()&&!String(payload.description??'').trim())return'Enter a Part Number or a clearly identifying product description.';return null}if(type==='general_goods'){if(Number(payload.estimatedBudget)<7500)return'The minimum estimated order value for this request type is USD 7,500.';for(const key of['productName','description','quantity','country','timeframe'])if(!String(payload[key]??'').trim())return'Complete all required general-goods fields.';return null}if(type==='project'){if(Number(payload.estimatedBudget)<40000)return'The minimum estimated project value is USD 40,000.';for(const key of['title','description','objective','equipment','engineeringScope','country','timeframe'])if(!String(payload[key]??'').trim())return'Complete all required project fields.';return null}return'Unsupported request type.'}

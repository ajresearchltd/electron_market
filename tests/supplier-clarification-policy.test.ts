import test from 'node:test';import assert from 'node:assert/strict';
import {clarificationIdempotencyKey,clarificationToken,classifyExplicitClarificationReply,needsSupplierClarification} from '../lib/supplier-email/clarification-policy.ts';
test('exact and formatting-only matches need no clarification',()=>{assert.equal(needsSupplierClarification({matchState:'matched_exact',matchedRfqItemId:'1'},'ABC123','ABC123'),false);assert.equal(needsSupplierClarification({matchState:'matched_normalized',method:'matched_normalized',matchedRfqItemId:'1'},'ABC-123','ABC123'),false)});
test('semantic, Nexar and missing-MPN unique matches require clarification',()=>{assert.equal(needsSupplierClarification({matchState:'matched_semantic',matchedRfqItemId:'1'},null,'IRF540N'),true);assert.equal(needsSupplierClarification({matchState:'matched_normalized',method:'matched_normalized_nexar',matchedRfqItemId:'1'},'VENDOR-X','ABC123'),true)});
test('ambiguous and unmatched results do not auto clarify',()=>{assert.equal(needsSupplierClarification({matchState:'ambiguous',matchedRfqItemId:null},null,'ABC'),false);assert.equal(needsSupplierClarification({matchState:'unmatched',matchedRfqItemId:null},null,'ABC'),false)});
test('clarification identity and explicit reply classification are stable',()=>{assert.equal(clarificationToken('item','rfq'),clarificationToken('item','rfq'));assert.match(clarificationToken('item','rfq'),/^CLAR-[A-F0-9]{12}$/);assert.equal(clarificationIdempotencyKey('item','rfq'),'item:rfq:1');assert.equal(classifyExplicitClarificationReply('CONFIRMED — correct'),'confirmed');assert.equal(classifyExplicitClarificationReply('NOT CONFIRMED — incorrect'),'rejected');assert.equal(classifyExplicitClarificationReply('please call us'),'ambiguous')});

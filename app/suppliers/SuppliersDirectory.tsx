'use client';
import{useEffect,useMemo,useState}from'react';import Link from'next/link';
type Supplier={slug:string;name:string;logoUrl:string|null;description:string|null;country:string|null;city:string|null;categories:string|null;specialization:string|null;productCount:number;verified:boolean};
const valid=(v:string|null)=>Boolean(v&&(v.startsWith('https://')||v.startsWith('http://')||v.startsWith('/')));
export default function SuppliersDirectory(){const[rows,setRows]=useState<Supplier[]>([]),[query,setQuery]=useState(''),[loading,setLoading]=useState(true),[failed,setFailed]=useState(false);useEffect(()=>{fetch('/api/public/suppliers').then(r=>r.ok?r.json():Promise.reject()).then(b=>setRows(b.suppliers??[])).catch(()=>setFailed(true)).finally(()=>setLoading(false))},[]);const shown=useMemo(()=>{const q=query.trim().toLowerCase();return q?rows.filter(s=>[s.name,s.country,s.city,s.categories,s.description,s.specialization].some(v=>v?.toLowerCase().includes(q))):rows},[rows,query]);return <><div className="mx-auto max-w-[1180px] px-4 py-12 sm:px-6 lg:px-8"><div className="rounded-3xl bg-gradient-to-br from-[#061b3f] to-[#0b3a7a] px-6 py-10 text-white shadow-xl sm:px-10"><span className="rounded-full bg-white/10 px-3 py-1 text-xs font-bold uppercase tracking-widest text-cyan-200">Electron Market network</span><h1 className="mt-4 text-3xl font-bold sm:text-4xl">Verified Suppliers</h1><p className="mt-3 max-w-2xl text-blue-100">Explore verified component suppliers available through Electron Market.</p></div><div className="mt-8"><label htmlFor="supplier-search" className="sr-only">Search suppliers</label><input id="supplier-search" type="search" value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search by supplier, country, city, category, or specialization" className="w-full rounded-xl border border-slate-300 bg-white px-4 py-3 shadow-sm outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-100"/></div>{loading?<p className="py-16 text-center text-slate-600">Loading verified suppliers…</p>:failed?<p className="py-16 text-center text-slate-600">Supplier directory is temporarily unavailable.</p>:shown.length===0?<p className="py-16 text-center text-slate-600">{rows.length?'No suppliers match your search.':'No public suppliers are currently available.'}</p>:<div className="mt-8 grid gap-6 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4">{shown.map(s=><article key={s.slug} className="flex min-h-[360px] flex-col overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm transition hover:-translate-y-1 hover:shadow-lg"><Link href={`/suppliers/${encodeURIComponent(s.slug)}`} className="flex h-40 items-center justify-center bg-slate-50 p-6">{valid(s.logoUrl)?<img src={s.logoUrl!} alt={`${s.name} logo`} className="h-full w-full object-contain"/>:<span className="flex h-24 w-24 items-center justify-center rounded-2xl bg-white text-3xl font-bold text-blue-700 shadow-sm">{s.name.slice(0,2).toUpperCase()}</span>}</Link><div className="flex flex-1 flex-col p-5"><span className="w-fit rounded-full bg-blue-50 px-2.5 py-1 text-xs font-bold text-blue-700">Verified Supplier</span><h2 className="mt-3 text-xl font-bold text-slate-950">{s.name}</h2>{(s.country||s.city)&&<p className="mt-1 text-sm text-slate-500">{[s.city,s.country].filter(Boolean).join(', ')}</p>}{s.description&&<p className="mt-3 line-clamp-3 text-sm leading-6 text-slate-600">{s.description}</p>}{(s.categories||s.specialization)&&<p className="mt-3 line-clamp-2 text-xs font-semibold text-slate-500">{s.categories||s.specialization}</p>}<Link href={`/suppliers/${encodeURIComponent(s.slug)}`} className="mt-auto pt-5 text-sm font-bold text-blue-700 hover:text-blue-900">View supplier →</Link></div></article>)}</div>}</div></>}

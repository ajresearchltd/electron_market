import {NextResponse} from 'next/server';
import {requireInternalApi} from '../../../../../../lib/auth/require-internal-api';
import {loadCanonicalInvoiceForDocument} from '../../../../../../lib/invoices/invoice-document';
import {getOrCreateInvoicePdf} from '../../../../../../lib/invoices/invoice-pdf-storage';
export async function GET(_:Request,{params}:{params:Promise<{invoiceId:string}>}){const auth=await requireInternalApi();if('error'in auth)return NextResponse.json({error:auth.error},{status:auth.status});if(auth.role!=='admin')return NextResponse.json({error:'Admin authorization is required.'},{status:403});const{invoiceId}=await params;try{const model=await loadCanonicalInvoiceForDocument(auth.admin,invoiceId),pdf=await getOrCreateInvoicePdf(model);return new NextResponse(new Uint8Array(pdf.buffer),{headers:{'Content-Type':'application/pdf','Content-Disposition':`attachment; filename="${pdf.filename}"`,'Cache-Control':'private, max-age=31536000, immutable','X-Content-Type-Options':'nosniff'}})}catch{console.error('Admin Invoice PDF failed:',{invoiceId,code:'invoice_pdf_unavailable'});return NextResponse.json({error:'Invoice PDF is not available.'},{status:500})}}

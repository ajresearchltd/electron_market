import{NextResponse}from'next/server';import{submitPublicEnquiry}from'../../../../../lib/public-request/access';
const headers={'cache-control':'private, no-store'};
export async function POST(request:Request){const body=await request.json().catch(()=>({}));try{const result=await submitPublicEnquiry(String(body.type??''),body.payload??{},body.submissionIdempotencyKey,body.context);return NextResponse.json(result,{status:result.ok?(result.idempotentReplay?200:201):422,headers})}catch{console.error({operation:'public_enquiry_submit',diagnosticCode:'PUBLIC_ENQUIRY_SUBMISSION_FAILED'});return NextResponse.json({error:'Request could not be submitted. Please try again later.',diagnosticCode:'REQUEST_SUBMISSION_FAILED'},{status:503,headers})}}

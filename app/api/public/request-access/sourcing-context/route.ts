import{NextResponse}from'next/server';import{createClient}from'../../../../../lib/supabase/server';import{createSourcingContext,SOURCING_CONTEXT_COOKIE,sourcingContextCookieOptions}from'../../../../../lib/public-request/sourcing-context';
export async function POST(){const session=await createClient(),{data:{user}}=await session.auth.getUser();if(!user)return NextResponse.json({error:'Authentication required.'},{status:401});const response=NextResponse.json({ok:true,authIntent:'homepage_sourcing'});response.cookies.set(SOURCING_CONTEXT_COOKIE,await createSourcingContext(user.id),sourcingContextCookieOptions);return response}

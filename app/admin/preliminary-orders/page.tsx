import {redirect}from'next/navigation';import{createClient}from'../../../lib/supabase/server';import{createRequiredAdminClient}from'../../../lib/supabase/admin';import PreliminaryToBomConversion from'../../components/admin/PreliminaryToBomConversion';import{preliminaryOrderNumber}from'../../../lib/admin/preliminary-orders';
const show=(v:unknown)=>v==null||v===''?'—':String(v);
export default async function PreliminaryOrdersPage(){const session=await createClient(),{data:{user}}=await session.auth.getUser();if(!user)redirect('/login');const role=await session.from('user_profiles').select('role').eq('id',user.id).maybeSingle();if(!['admin','support'].includes(role.data?.role??''))redirect('/');const db=createRequiredAdminClient(),enquiries=await db.from('public_sourcing_enquiries').select('*').not('customer_user_id','is',null).order('created_at',{ascending:false}).limit(250),ids=[...new Set((enquiries.data??[]).map((x:any)=>x.customer_user_id).filter(Boolean))],[users,companies]=await Promise.all([ids.length?db.from('user_profiles').select('id,full_name,company_name,email').in('id',ids):Promise.resolve({data:[]}),ids.length?db.from('customer_company_profiles').select('user_id,company_name').in('user_id',ids):Promise.resolve({data:[]})]),userMap=new Map((users.data??[]).map((x:any)=>[x.id,x])),companyMap=new Map((companies.data??[]).map((x:any)=>[x.user_id,x]));return <main className="min-h-screen bg-slate-100 p-4 sm:p-8"><div className="mx-auto max-w-[1500px]"><header className="rounded-2xl bg-blue-950 p-5 text-white shadow-xl"><p className="text-xs font-bold uppercase tracking-wider text-cyan-300">Admin HUB</p><h1 className="mt-1 text-3xl font-bold">Preliminary Orders</h1><a href="/admin" className="mt-4 inline-flex rounded-lg border border-white/30 px-4 py-2 text-sm font-bold">Back to Admin HUB</a></header><section className="mt-6 overflow-x-auto rounded-2xl bg-white shadow"><table className="min-w-[1350px] w-full text-left text-sm"><thead className="bg-slate-900 text-white"><tr>{['DETAILS','REQUEST NO','RECEIVED','CUSTOMER','COMPANY','EMAIL','REQUEST TYPE','SUMMARY','ESTIMATED VALUE','COUNTRY','STATUS','ACTION'].map(x=><th key={x} className="px-3 py-3">{x}</th>)}</tr></thead><tbody>{(enquiries.data??[]).map((row:any)=>{const profile:any=userMap.get(row.customer_user_id),company:any=companyMap.get(row.customer_user_id),payload=row.payload??{},converted=Boolean(row.converted_bom_upload_id);return <tr key={row.id} className="border-t align-top"><td className="px-3 py-3"><details><summary className="cursor-pointer font-bold text-blue-700">View</summary><pre className="mt-3 max-w-xl whitespace-pre-wrap rounded-lg bg-slate-100 p-3 text-xs">{JSON.stringify(payload,null,2)}</pre></details></td><td className="px-3 py-3 font-mono text-xs">{preliminaryOrderNumber(row)}</td><td className="px-3 py-3">{new Date(row.created_at).toLocaleString()}</td><td className="px-3 py-3">{show(profile?.full_name)}</td><td className="px-3 py-3">{show(company?.company_name||profile?.company_name)}</td><td className="px-3 py-3">{show(row.contact_email)}</td><td className="px-3 py-3">{show(row.request_type)}</td><td className="max-w-56 px-3 py-3">{show(payload.description||payload.productName||payload.title||payload.partNumber)}</td><td className="px-3 py-3">{show(payload.estimatedBudget)}</td><td className="px-3 py-3">{show(payload.country||payload.destinationCountry)}</td><td className="px-3 py-3">{converted?'Converted to BOM List':show(row.status)}</td><td className="px-3 py-3">{converted?<a className="font-bold text-blue-700" href={`/admin/procurement-progress/${row.converted_procurement_chain_id}#bom`}>Open BOM List</a>:<PreliminaryToBomConversion orderId={row.id} customerId={row.customer_user_id} compact/>}</td></tr>})}</tbody></table>{!enquiries.data?.length&&<p className="p-8 text-center text-slate-500">No Preliminary Orders have been submitted.</p>}</section></div></main>}
